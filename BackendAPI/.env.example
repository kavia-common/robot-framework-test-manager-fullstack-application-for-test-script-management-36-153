# Core configuration (supply real values in deployment)
DATABASE_URL=postgresql+psycopg2://user:password@db:5432/robot_test_manager
JWT_SECRET=change_me
JWT_EXPIRES_IN=60

# Minio (S3-compatible) settings
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=rf-test-manager

# CORS origins (comma-separated). Use * for development
CORS_ALLOW_ORIGINS=*
